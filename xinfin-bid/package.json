{
  "name": "xinfin-bid",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ xinfin-bid",
    "create-local": "graph create --node http://localhost:8020/ xinfin-bid",
    "remove-local": "graph remove --node http://localhost:8020/ xinfin-bid",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 xinfin-bid"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
